const express = require('express');

const router = express.Router();

// Agrega al usuario que realizo la solicitud, a la lista de miembros de la alerta
router.get('/:alertID', (req, res) => {

});

module.exports = router;